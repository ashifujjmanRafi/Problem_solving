#include <bits/stdc++.h>
using namespace std;
int main()
{
	
	int tc,max1=0,remind=0;
	vector<string> v;
	vector<int> x;
	string s;
	int n;
 
	cin>>tc;
 
	for (int i = 0; i < tc; ++i)
	{
		cin>>s>>n;
		v.push_back(s);
		x.push_back(n);
	}
	
}